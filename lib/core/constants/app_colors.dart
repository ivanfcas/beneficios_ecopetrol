import 'package:flutter/material.dart';

// Clase para centralizar los colores corporativos de Ecopetrol.
// Si los colores cambian, solo necesitas modificarlos en este archivo.
class AppColors {
  // Color primario (Verde Ecopetrol)
  // Placeholder - Reemplazar si el código hexadecimal es diferente.
  static const Color primaryGreen = Color(0xFF00A859);

  // Color secundario (Amarillo Ecopetrol)
  // Placeholder - Reemplazar si el código hexadecimal es diferente.
  static const Color primaryYellow = Color(0xFFFFC72C);

  // Colores para textos
  static const Color darkText = Color(0xFF333333);
  static const Color lightText = Color(0xFF666666);

  // Color de fondo claro para tarjetas o secciones
  static const Color lightBackground = Color(0xFFF5F5F5);
}
